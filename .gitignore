# Secrets — never commit these. In production they live as GitHub Actions secrets.
.env
.env.*
*.local

# Clip video files (staged to R2, then deleted — never committed)
*.mp4
*.jpg

# Generated caption subtitle files (built per-post in cwd, burned in, never committed)
*.ass
*.wav

# Internal work order — do not publish to the public repo
clipkroniek/PLAN.md

# Python
__pycache__/
*.pyc
.venv/
venv/
env/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
